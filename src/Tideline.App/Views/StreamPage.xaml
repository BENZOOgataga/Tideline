<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Tideline.App.Views.StreamPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:Tideline.App.ViewModels">
    <Grid RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Stream" Style="{ThemeResource TitleTextBlockStyle}" />
            <TextBlock Text="Every note, newest at the bottom."
                       Style="{ThemeResource BodyTextBlockStyle}"
                       Opacity="0.7" />
        </StackPanel>

        <AutoSuggestBox x:Name="SearchBox"
                        Grid.Row="1"
                        PlaceholderText="Search notes"
                        QueryIcon="Find"
                        TextChanged="SearchBox_TextChanged"
                        QuerySubmitted="SearchBox_QuerySubmitted" />

        <ScrollViewer Grid.Row="2" x:Name="StreamScroll">
            <ItemsControl x:Name="StreamItems">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:NoteCard">
                        <Border Margin="0,6"
                                Padding="12"
                                Background="{ThemeResource LayerFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="6">
                            <StackPanel Spacing="4">
                                <TextBlock Text="{x:Bind Body}" TextWrapping="Wrap" />
                                <TextBlock Text="{x:Bind Framing}"
                                           Style="{ThemeResource CaptionTextBlockStyle}"
                                           Opacity="0.55" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
